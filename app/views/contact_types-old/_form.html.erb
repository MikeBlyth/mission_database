<%= form_for(@contact_type) do |f| %>
  <% if @contact_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_type.errors.count, "error") %> prohibited this contact_type from being saved:</h2>

      <ul>
      <% @contact_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
