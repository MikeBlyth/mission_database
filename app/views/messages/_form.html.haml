%p.msg-caveat
  Note: Be sure to follow up any messages you send with this system, and do not
  assume that they are being delivered reliably (yet).
#msg-form
  #msg-top
    = render :partial => 'text_inputs'
  #msg-bottom
    #groups
      = render :partial => 'groups'      
    #advanced 
      = render :partial => 'options'

