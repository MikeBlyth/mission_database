<h1>Listing bloodtypes</h1>

<table>
  <tr>
    <th>Abo</th>
    <th>Rh</th>
    <th>Comment</th>
    <th>Full</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bloodtypes.each do |bloodtype| %>
  <tr>
    <td><%= bloodtype.abo %></td>
    <td><%= bloodtype.rh %></td>
    <td><%= bloodtype.comment %></td>
    <td><%= bloodtype.full %></td>
    <td><%= link_to 'Show', bloodtype %></td>
    <td><%= link_to 'Edit', edit_bloodtype_path(bloodtype) %></td>
    <td><%= link_to 'Destroy', bloodtype, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bloodtype', new_bloodtype_path %>
