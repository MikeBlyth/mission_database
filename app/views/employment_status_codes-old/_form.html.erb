<%= form_for(@employment_status) do |f| %>
  <% if @employment_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employment_status.errors.count, "error") %> prohibited this employment_status from being saved:</h2>

      <ul>
      <% @employment_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
