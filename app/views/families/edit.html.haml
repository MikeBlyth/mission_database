.active-scaffold
  %h1 Edit Family 
  = form_for(@family) do |f| 
    - @record = @family
    = render 'error_messages', :object => @errors
    = render 'form' , :formobject=>f
    .actions
    = f.submit 
    = link_to "Cancel", users_path, :class=>"as_cancel"
    
